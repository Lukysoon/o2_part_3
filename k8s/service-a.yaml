# Service A - vstupni bod aplikace, NodePort pro pristup zvenku clusteru
apiVersion: v1
kind: Service
metadata:
  name: service-a
spec:
  type: NodePort
  selector:
    app: a
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
